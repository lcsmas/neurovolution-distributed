{
  "name": "neuro",
  "version": "1.0.0",
  "description": "Self learning flappy bird server, it persist neural network generated by client and distribute it to the web. The aim of the application is to have a lot of client running the AI to increase the learning speed by sharing the algorithm.",
  "main": "neuro.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Lucas MAS",
  "license": "ISC",
  "dependencies": {
    "cassandra-driver": "^4.1.0",
    "cors": "^2.8.5",
    "express": "^4.17.0",
    "uuid": "^3.3.2"
  }
}
