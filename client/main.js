import { Population } from './neurolib/population.js';

const options = { 
    individualNumber : 2,
    inputLayerSize : 2,
    hiddenLayerSize : 2,
    outputLayerSize : 1
}

const population = new Population( options );

